<div class="container-photo-bar">
  <%= image_tag "bar.jpg", alt: "bar" %>
  <% @cocktails.each do |cocktail| %>
  <h2><%= link_to cocktail.name, cocktail_path(cocktail) %></h2>
  <% end %>
  <h3><%= link_to "create a new cocktail", new_cocktail_path %></h3>
</div>