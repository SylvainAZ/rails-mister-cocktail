<h2><%= @cocktail.name %></h2>
<% @cocktail.doses.each do |dose| %>
<p>
  dose :
  <%= dose.description %> of
  <%= dose.ingredient.name %>
</p>
<% end %>
<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary"%>
<h3><%= link_to "go to index", cocktails_path, class: "btn btn-primary" %></h3>